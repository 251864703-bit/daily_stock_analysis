name: 每日股票分析

on:
  schedule:
    - cron: '0 10 * * 1-5'
  workflow_dispatch:
    inputs:
      mode:
        description: '模式'
        required: true
        default: 'stocks-only'
        type: choice
        options: [full, market-only, stocks-only]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 安装核心依赖
        run: |
          pip install --upgrade pip
          pip install openai pandas yfinance requests akshare efinance
          [ -f requirements.txt ] && (grep -v "pandas_ta" requirements.txt > req_new.txt && pip install -r req_new.txt || echo "skip")

      - name: 创建目录
        run: mkdir -p data logs reports
      
      - name: 执行分析 (精准对齐模型名)
        env:
          OPENAI_API_KEY: ${{ secrets.DEEP_SEEK_KEY }}
          OPENAI_BASE_URL: "https://api.deepseek.com"
          
          # 【关键修正】尝试三种可能的变量名，确保程序能认出 deepseek-chat
          OPENAI_MODEL: "deepseek-chat"
          OPENAI_MODEL_NAME: "deepseek-chat"
          MODEL_NAME: "deepseek-chat"
          
          # 免账号行情配置
          REALTIME_SOURCE_PRIORITY: "tencent,akshare_sina,efinance"
          TUSHARE_TOKEN: "" 
          
          # 股票清单
          STOCK_LIST: ${{ vars.STOCK_LIST || secrets.STOCK_LIST || '600519' }}
        run: |
          # 运行主程序，不带任何它不认识的参数
          python main.py --no-market-review

      - name: 结果展示
        if: always()
        run: |
          echo "=========================================="
          echo "📝 检查分析报告内容..."
          find reports -name "*.md" -exec cat {} + || echo "未发现 Markdown 报告"
          echo "=========================================="
      
      - name: 上传所有产出
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stock-results
          path: |
            reports/
            logs/
